function loopTitle() {
    displayTitle()
}

function displayTitle() {
    drawSceneInit()

    context.fillText(`Puzzle Platformer`, UI.title.textTitle[0], UI.title.textTitle[1])
}

function keyDownTitle(key) {
    if (menu === false) {
        if (state === '') {
            
        }
    }
}

function keyUpTitle(key) {

}