function loopTitle() {
    renderTitle()
}

function renderTitle() {
    renderInit()
}

function mouseUpTitle(x, y, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === '') {
                
            }
        }
    }
}

function keyDownTitle(key) {
}
